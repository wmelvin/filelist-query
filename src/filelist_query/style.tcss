
Screen {
    align: center top;
}

Static {
    content-align: center middle;
}

.tab-content {
    background: $success;
}

/* FieldsTab */

FieldsTab OptionList {
    width: 20;
    height: auto;
    margin: 2 2;
}

FieldsTab Button {
    width: 10;
}

.buttonbar {
    align: center middle;
    width: 14;
}

.active {
    background: $success;
    color: $text;
}

/* CriteriaTab */

CriteriaTab ScrollableContainer {
    margin: 0;
    width: 100%;
}

Predicate {
    layout: horizontal;
    content-align: left middle;
}

Predicate Select {
    margin: 1;
}

Predicate #select_pred {
    width: 10;
}

Predicate #select_field {
    width: 20;
}

Predicate #select_condition {
    width: 30;
}

Predicate Input {
    margin: 1;
    width: 40;
}

#select_pred {
    visibility: hidden;
}

.added #select_pred {
    width: 16;
    margin: 1;
    visibility: visible;
}

/* ResultsTab */

ResultsTab {
    margin: 0;
    padding: 0;
}

#results-table {
    height: 1fr;
}
