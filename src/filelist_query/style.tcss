
Screen {
    align: center top;
}

Static {
    content-align: center middle;
}

.tab-content {
    background: $success;
}

/* ColumnsTab */

ColumnsTab OptionList {
    width: 20;
    height: auto;
    margin: 2 2;
}

ColumnsTab Button {
    width: 10;
}

.buttonbar {
    align: center middle;
    width: 14;
}

.active {
    background: $success;
    color: $text;
}

/* CriteriaTab */

CriteriaTab ScrollableContainer {
    margin: 0;
    width: 1fr;
}

Predicate {
    layout: horizontal;
    margin: 0;
    padding: 0;
    content-align: left middle;
}

Predicate Select {
    margin: 0;
    padding: 0;
}

Predicate Button {
    border: solid darkgray;
}

Predicate #select-pred {
    width: 1fr;
}

Predicate #select-column {
    width: 2fr;
}

Predicate #select-condition {
    width: 3fr;
}

Predicate Input {
    margin: 0;
    width: 4fr
}

Predicate #add-predicate {
    margin: 0;
    max-width: 5;
    color: green;
}

Predicate #remove-predicate {
    margin: 0;
    max-width: 5;
    color: red;
}

#remove-predicate {
    visibility: hidden;
}

.added #remove-predicate {
    visibility: visible;
}

#select-pred {
    visibility: hidden;
}

.added #select-pred {
    visibility: visible;
}

/* ResultsTab */

ResultsTab {
    margin: 0;
    padding: 0;
}

#results-table {
    height: 1fr;
}

/* HistoryScreen */

HistoryScreen Horizontal {
    align: center middle;
    height: 1fr;
}
#load-button, #close-button {
    border: solid gray;
}
#sql-text {
    height: 2fr;
}
#history-list {
    height: 4fr;
}
